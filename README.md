# 🧹 CleanSweep — Умная и безопасная очистка системы

**CleanSweep** — это десктопное приложение на Python для анализа и удаления временных файлов, кэша и ненужных данных **в вашем домашнем каталоге**.  
Также включает **безопасное управление автозагрузкой**, что реально ускоряет запуск компьютера.

> ✨ Полностью реализовано на стандартной библиотеке Python — без внешних зависимостей.  
> 🔒 Никаких рисков: нет доступа к системным файлам, реестру или критическим компонентам ОС.

---

## 🚀 Основные возможности

### 🗑️ Очистка мусора
- Автоматическое сканирование домашнего каталога (`/home/user`, `C:\Users\user`, и т.д.)
- Поиск:
  - Временных файлов (`*.tmp`, `*.log`, `*.bak`)
  - Кэша Python (`__pycache__`, `*.pyc`)
  - Системного мусора (`.DS_Store`, `Thumbs.db`)
  - Пустых папок
- **Группировка результатов**:
  - 🔴 **Потенциально важные** — файлы проектов (`.py`, `.ipynb`, `requirements.txt` и др.)
  - 🟢 **Безопасный мусор** — точно временные данные
- Отдельное **предупреждение при удалении важных файлов**

### ⚡ Управление автозагрузкой (Windows)
- Просмотр всех программ, запускаемых при старте системы
- Безопасное **отключение автозагрузки** (без удаления файлов!)
- Реальный прирост скорости загрузки ОС

### 🎨 Современный интерфейс
- Чистый, минималистичный дизайн
- Анимация сканирования (вращающаяся лупа)
- Адаптивная верстка — интерфейс не ломается при изменении размера окна
- Плавная прокрутка без лагов

---

## 🛠️ Технические детали

- **Язык**: Python 3.7+
- **Зависимости**: отсутствуют (только `tkinter`, `os`, `pathlib`, `winreg` на Windows)
- **Безопасность**:
  - Сканирование ограничено домашним каталогом
  - Удаление только пустых папок (`os.rmdir`)
  - Подтверждение перед удалением важных файлов
  - Управление автозагрузкой — только через пользовательские ключи реестра (`HKCU`)
- **Кроссплатформенность**: основная функция работает на Windows, macOS, Linux  
  *(автозагрузка — только на Windows)*

---

## ▶️ Запуск

1. Убедитесь, что установлен **Python 3.7 или новее**.
2. Скачайте репозиторий:
   ```bash
   git clone https://github.com/your-username/cleansweep.git
   cd cleansweep